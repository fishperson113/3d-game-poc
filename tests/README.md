# Browser integration tests

Unit and headless tests live beside their source modules. This folder is reserved for browser-level integration.

TODO(plan-05): Cover load → build → simulate → goal/reset with Playwright.
