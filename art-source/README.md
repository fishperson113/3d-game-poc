# Art source

Reference images and authoring briefs for approved parts live under `parts/<part-id>/`.

Plan 04 uses internal procedural briefs for the first block visuals. No external image reference is shipped yet; the briefs are the source and explicitly record that limitation. The pinned img2threejs commit is retained in each manifest as authoring-tool provenance, while the runtime factories are reviewed procedural output.
